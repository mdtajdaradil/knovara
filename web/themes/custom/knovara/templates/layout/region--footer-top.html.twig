{#
    /**
     * @file
     * Footer top region for the Knovara theme.
     *
     * The default region wrapper is intentionally removed so that
     * footer blocks become direct children of .footer-grid.
     */
    #}
    
    {% if content %}
      {{ content }}
    {% endif %}